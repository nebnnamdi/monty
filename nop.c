#include "monty.h"
/**
 *nop - function to do nothing
 *@stack: pointer to to pointer to doubly linked list
 *@line_number: number of lines in file
 *Return: nothing
 */
void nop(stack_t **stack, unsigned int line_number)
{
(void) stack;
(void) line_number;
return;
}
